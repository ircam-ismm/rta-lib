function [fft] = rta_fft (input, setup)
% function [fft] = rta_fft (input, setup)
%
% Compute the <fft> of the real <input> signal using the RTA library.
%
% <input> is a real signal vector which size must not be greater than the
% <fft> size used to define the setup (see rta_fft_setup_new).
%
    
['This file is an help file which relies on a mex file of the same' ...
 ' name (but with a different extension).\n']
%
% 2008 (C) Ircam - Centre Pompidou
% Jean-Philippe.Lambert@ircam.fr
