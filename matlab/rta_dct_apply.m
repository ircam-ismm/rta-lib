function [dct] = rta_dct_apply (input, weights)
% function [dct] = rta_dct_apply (input, weights)
%
% Apply a <weights> matrix to an <input> vector. <weights> must can be
% created by the rta_dct_weights function and the sizes must fit.
%
% <dct> = <input>' * <weights>
    
['This file is an help file which relies on a mex file of the same' ...
 ' name (but with a different extension).\n']
%
% 2008 (C) Ircam - Centre Pompidou
% Jean-Philippe.Lambert@ircam.fr
